<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/4/minty/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="{{path('home')}}">Lettre au pere noel</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor02">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{path('newLettre')}}">ecrire une lettre</a>
      </li>
      {% if not app.user  %}


      <li class="nav-item">
        <a class="nav-link" href="{{path('security_login')}}">connexion</a>
      </li>

       {% else %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('security_registration')}}">Ajouter membre</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('lettre_liste')}}">liste des lettres</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('ecrirereponse')}}">Repondre</a>
            </li>
            <li class="nav-item">
        <a class="nav-link" href="{{path('security_logout')}}">Deconnexion</a>
      </li>
            {% endif %}
    </ul>
  </div>
</nav>
<div class="container">
</div>
        {% block body %}

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
